<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.example.tripnote.dao.UserDAO">
<select id="login" resultType="UserDTO" parameterType="String">
   select id, username, password, email, name, nickname, profile_image
   from user
   where username = #{username}
   AND password = #{password}
</select>
<select id="searchId" resultType="UserDTO" parameterType="String">
   select username
   from user
   where email = #{email}
</select>
</mapper>