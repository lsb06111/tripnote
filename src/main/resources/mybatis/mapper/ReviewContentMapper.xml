<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.example.tripnote.dao.ReviewContentDAO">

<insert id="save" parameterType="list" >
	INSERT INTO review_content (review_content, img, tour_loc_id)
        <foreach collection="list" item="item" separator="UNION ALL">
            SELECT #{item.content}, #{item.imgSrc}, #{item.tourLocId} 
            FROM DUAL
		</foreach>
</insert>

<insert id="saveDraft" parameterType="list" >
	INSERT INTO review_content (review_content, img, tour_loc_id, is_draft)
        <foreach collection="list" item="item" separator="UNION ALL">
            SELECT #{item.content}, #{item.img}, #{item.tourLocId} , 1
            FROM DUAL
		</foreach>
</insert>

</mapper>