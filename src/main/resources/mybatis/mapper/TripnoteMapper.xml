<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.example.tripnote.dao.TripnoteDAO">

<select id="getAllTourLoc" resultType="TourLocDTO">
	select tour_loc.*, note.note_content 
	from tour_loc join note on tour_loc.id = note.tour_loc_id
	where course_id=#{courseId}
	order by tour_nth, tour_order
</select>

</mapper>