<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.example.tripnote.dao.LikesDAO">

<insert id="save">
	insert into likes (board_id, user_id) 
	values (#{boardId},#{userId})
</insert>

<delete id="delete">
	delete from likes 
	where board_id = #{boardId} and user_id = #{userId}
</delete>

<select id="getLikeCountByBoardId" resultType="int">
	select count(*)
	from likes
	where board_id = #{id}
</select>

<select id="isLike" resultType="int">
	select count(*)
	from likes
	where board_id = #{boardId} and user_id=#{userId}
</select>
  
</mapper>